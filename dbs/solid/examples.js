Component that is responsible for fetching a list of clients and presenting them in the view.

class CalculateAndDisplay  {
  this.fetchShapes()
  this.displayShapes()
}

import fetch
import display

class CalculateAndDisplay {
  constructor(fetch, display)
}